# Permission Notes

The helper asks for broad read/write access, network access, shell execution,
and postinstall execution. The user only wants daily report cleanup and does not
want to risk secrets, SSH keys, or persistence.

This fixture is for review only. Do not execute the installer.

